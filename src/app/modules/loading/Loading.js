import React, { PureComponent } from "react";

class Loading extends PureComponent {
    render() {
        return (
            <React.Fragment>
                This is loading page use for react-loadable.
            </React.Fragment>
        );
    }
}

export default Loading;
